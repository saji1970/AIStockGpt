# Local React dev: admin page is training-pipeline only
REACT_APP_ADMIN_MODE=training-only
# Use Railway API so login matches the DB where admin@admin.com was created
REACT_APP_API_URL=https://aistockgpt-production.up.railway.app
